<dependencies>
  <androidPackages>
    <!-- Java package with no native shared library dependencies. -->
    <androidPackage spec="com.android.support:support-annotations:26.1.0"/>
    <!-- Java package with no native shared library dependencies. -->
    <androidPackage spec="com.google.firebase:firebase-app-unity:5.1.1">
      <repositories>
        <repository>Assets/Firebase/m2repository</repository>
      </repositories>
    </androidPackage>
    <!-- Java package specified with a classifier. -->
    <androidPackage spec="org.test.psr:classifier:1.0.1:foo@aar"/>
    <!-- Global repository. -->
    <repositories>
      <repository>file:///my/nonexistant/test/repo</repository>
      <repository>project_relative_path/repo</repository>
    </repositories>
  </androidPackages>
</dependencies>
